# Dev fallback — loaded automatically from project root .env on startup.
# You can also save a key in the Polyflo Settings UI (OS keyring).
SARVAM_API_KEY=your_sarvam_api_key_here

# Rust log level (optional)
RUST_LOG=info,polyflo=debug
